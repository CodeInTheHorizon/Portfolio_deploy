# K Multiverse Portfolio

An original cinematic React portfolio themed around magical dimensional travel.

## Run locally

```powershell
npm.cmd install --cache .npm-cache
npm.cmd run dev
```

Use `npm.cmd run build` to create the production bundle in `dist/`.
